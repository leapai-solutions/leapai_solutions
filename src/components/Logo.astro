---
import { SITE } from 'astrowind:config';
import logo from '~/assets/images/logo.svg?raw';
---

<span
  class="inline-flex h-10 md:h-12 [&>svg]:h-full [&>svg]:w-auto"
  aria-label={SITE?.name || 'LeapAI Solutions'}
  role="img"
  set:html={logo}
/>
